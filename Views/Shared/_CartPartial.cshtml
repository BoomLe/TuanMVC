@using Microsoft.AspNetCore.Http

@inject CartService _carService

@{
    var cart = _carService.GetCartItems();

    if(cart.Count > 0)
    {
        <div class="nav-item">
            <a asp-route="cart" >
                Giỏ hàng(@cart.Count)
        </a>
        </div>
    }
}